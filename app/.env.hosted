# npm run dev:hosted is FANatical's explicit localhost-to-production opt-in.
# Put the hosted URL and browser-safe publishable key in .env.hosted.local or
# retain an existing generic .env.local file. Both local files are gitignored.
VITE_SUPABASE_BACKEND=hosted
VITE_ALLOW_HOSTED_SUPABASE_DEV=true
